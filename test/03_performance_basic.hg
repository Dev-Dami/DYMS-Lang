// speedtest.hg
// Simple speed test for DYMS interpreter

println("== DYMS Speed Test ==")

// Sum of first N numbers
let N = 100000
let sum = 0

let start = 0
let end = 0

// crude timer using for-loops (since we don’t have built-in time yet)
for range(i, 1000000) { start = i } // warm-up

for range(i, N) {
    sum = sum + i
}

println("Sum of 1.." + N + " = " + sum)

// Nested loop test
let counter = 0
for range(i, 500) {
    for range(j, 500) {
        counter = counter + 1
    }
}

println("Nested loop counter = " + counter)
println("Done.")
